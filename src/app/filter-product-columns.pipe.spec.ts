import { FilterProductColumnsPipe } from './filter-product-columns.pipe';

describe('FilterProductColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
